# HTTP VirtualHost (Port 80) - Redirect to HTTPS
<VirtualHost *:80>
    ServerName openmosaics.com
    DocumentRoot /var/www/html

    RewriteEngine On
    RewriteCond %{SERVER_NAME} =openmosaics.com
    RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>

# HTTPS VirtualHost (Port 443) - Simplified without WebSocket
<IfModule mod_ssl.c>
<VirtualHost *:443>
    ServerName openmosaics.com
    DocumentRoot /var/www/html

    # SSL Configuration
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/openmosaics.com/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/openmosaics.com/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf

    # Proxy Configuration
    ProxyRequests Off
    ProxyPreserveHost On
    ProxyAddHeaders On

    # Main application proxy (no WebSocket needed)
    ProxyPass / http://127.0.0.1:8080/
    ProxyPassReverse / http://127.0.0.1:8080/
</VirtualHost>
</IfModule>
